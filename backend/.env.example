# Local development — copy to .env and adjust values
DEBUG=True
SECRET_KEY=change-me-for-local-dev

# MongoDB (local or Atlas)
MONGODB_URI=mongodb://localhost:27017
MONGODB_NAME=smart_certificate_db

# Optional admin bootstrap (also used by ensure_admin on deploy)
ADMIN_USERNAME=Goku.S
ADMIN_PASSWORD=Tech@123
